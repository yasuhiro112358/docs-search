{
  "name": "docs_search",
  "version": "1.2.0",
  "description": "DocsSearch",
  "scripts": {
    "clean": "rm -rf dist/*",
    "build": "npm run clean && cp -r src/* dist/",
    "zip": "cd dist && zip -r ../releases/docs_search_latest.zip *",
    "release": "npm run build && npm run zip"
  },
  "devDependencies": {
    "vite": "^5.4.8"
  }
}
